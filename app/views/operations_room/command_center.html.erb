<% if current_user.suggest_owner_guide || current_user.suggest_service_provider_guide %>
  <%= render 'operations_room/guide_main' %>
<% end %>
<% if !current_user.suggest_owner_guide || !current_user.suggest_service_provider_guide %>
  <%= render 'operations_room/dashboard' %>
<% end %>
