<div class="p-2 w-full">
  <div class="my-2 d-flex align-items-center justify-content-start">
    <div class="flex-fill"></div>
    <div class="form-floating w-[300px]">
      <input type="search" placeholder="Search" name="or_search_staffs" class="form-control">
      <label for="search">Search your staffs</label>
    </div>
  </div>
  <div>
    <table class="table table-condensed table-hover table-striped table-fixed table-responsive">
      <thead>
        <tr>
          <th>Staff</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% @staffs.map do |staff| %>
          <tr>
            <td><%= staff.name %></td>
            <td>
              <%= link_to "visibility", staff_path(staff), class: "material-icons" %>
              <%= link_to "edit", edit_staff_path(staff), class: "material-icons text-red-500 hover:text-red-800" %>
              <%= link_to "delete", staff_path(staff), method: :delete, class: "material-icons text-red-500 hover:text-red-800" %>
            </td>
          </tr>
        <% end %>
      </tbody>
      <tfoot></tfoot>
    </table>
  </div>
  <div class="my-2 d-flex align-items-center justify-content-start">
    <%= paginate @staffs %>
  </div>
</div>
