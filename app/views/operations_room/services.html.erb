<div class="p-2 w-full">
  <div class="my-2 d-flex align-items-center justify-content-start">
    <div class="flex-fill"></div>
    <div class="form-floating w-[300px]">
      <input type="search" placeholder="Search" name="or_search_services" class="form-control">
      <label for="search">Search your services</label>
    </div>
  </div>
  <div>
    <table class="table table-condensed table-hover table-striped table-fixed table-responsive">
      <thead>
        <tr>
          <th>Service</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <% @services.map do |service| %>
          <tr>
            <td><%= service.name %></td>
            <td>
              <%= link_to "visibility", service_path(service), class: "material-icons" %>
              <%= link_to "edit", edit_service_path(service), class: "material-icons text-red-500 hover:text-red-800" %>
              <%= link_to "delete", service_path(service), method: :delete, class: "material-icons text-red-500 hover:text-red-800" %>
            </td>
          </tr>
        <% end %>
      </tbody>
      <tfoot></tfoot>
    </table>
  </div>
  <div class="my-2 d-flex align-items-center justify-content-start">
    <%= paginate @services %>
  </div>
</div>
